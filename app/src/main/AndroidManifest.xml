<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jobsearch">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SlashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity"></activity>
        <activity android:name=".activities.RegistrationActivity"></activity>
        <activity android:name=".activities.MyProfileActivity"></activity>
        <activity android:name=".activities.ChangePasswordActivity"></activity>
        <activity android:name=".activities.EditYourProfileActivity"></activity>
        <activity android:name=".activities.SearchRelatedJobsActivity"></activity>
        <activity android:name=".activities.NewJobsActivity"></activity>
        <activity android:name=".activities.JobDetailsActivity"></activity>
        <activity android:name=".activities.ScheduleAppointmentActivity"></activity>
        <activity android:name=".activities.FillTheDetailsFormActivity"></activity>

    </application>

</manifest>